const firstSide = 10;
const secondSide = 20;
const thirdSide = 5;

console.log('The area of a triangle is the sum of all its sides. Our triangle has three sides: 10, 20, and 5.');
console.log('Perimeter = First Side + second side + third side')

const perimeter = firstSide + secondSide + thirdSide;

let perimeterTriangle = `Perimeter is ${perimeter}`;

console.log(perimeterTriangle);